[build-system]
requires = ["sip >=6, <7"]
build-backend = "sipbuild.api"

[project]
name = "talipot"
requires-python = ">=3.11"

[tool.sip.project]
abi-version="@SIP_ABI_VERSION@"
sip-include-dirs = ["../stl"]
sip-module = "talipot.sip"

[tool.sip.bindings.talipot]
concatenate = 12
